{
  "name": "@context-manager/core",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && mkdir -p dist/prompts && cp -f src/prompts/*.txt dist/prompts/",
    "dev": "tsc --watch",
    "exclusion-test": "node ./tests/run-exclusions.mjs",
    "pipeline-eval": "node ./tests/run-pipeline-eval.mjs"
  },
  "dependencies": {
    "@context-manager/config": "workspace:*",
    "dotenv": "^16.4.7",
    "screenshot-desktop": "^1.15.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
